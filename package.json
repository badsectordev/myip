{
  "name": "ip-lookup-worker",
  "version": "0.1.0",
  "description": "Cloudflare Worker for IP lookups with RDAP information",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^3.1.0",
    "wrangler": "^3.15.0"
  }
}
